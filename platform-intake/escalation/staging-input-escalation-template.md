# Staging Input Escalation

The staging base URL package is overdue or invalid.

Current impact:
- repo is restored
- Android and backend source trees are restored
- Android x backend rerun is still blocked only by missing/invalid staging URLs

Required correction:
- provide all 7 URLs in one validated manifest
