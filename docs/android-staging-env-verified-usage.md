# Android Staging Env Verified Usage

Only the verified export file may be used for official Android x backend integration reruns:
- `.env.staging.mobile`

The candidate probe file:
- `.env.staging.mobile.candidate`

is exploratory only and must not be treated as a verified staging configuration.
