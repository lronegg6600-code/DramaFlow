提醒：DramaFlow blocker `{{blocker_id}}` 仍未在 SLA 内收到有效输入。

- owner: `{{owner}}`
- 需要提供: `{{required_input}}`
- 投递位置: `platform-intake/received/`
- 验收命令: `backend/deployments/scripts/validate_received_inputs.sh`
- 不提供会阻塞: `{{impact}}`
