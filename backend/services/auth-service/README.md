# auth-service

Responsibilities in Phase 3:

- guest session bootstrap
- refresh token exchange
- current user summary lookup

Future Google, Apple, and email login providers will plug into the service layer without changing the existing transport routes.
