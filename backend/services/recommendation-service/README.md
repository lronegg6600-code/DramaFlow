# recommendation-service

Reserved for recall, ranking, experiment bucketing, and candidate generation.

Phase 3 feed ranking stays rule-based inside `feed-service`. This service placeholder keeps a clean seam for later migration to asynchronous pipelines and model-backed ranking.
