# backend tools

Reserved for migration helpers, OpenAPI aggregation scripts, load generators, and local bootstrap tooling.

Phase 3 intentionally keeps the tool surface small and relies on `Makefile` plus SQL seed files.
