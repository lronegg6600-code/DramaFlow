{{- if .Values.services.content.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "dramaflow-backend.fullname" . }}-content
spec:
  replicas: {{ .Values.services.content.replicas }}
  selector:
    matchLabels:
      app: {{ include "dramaflow-backend.fullname" . }}-content
  template:
    metadata:
      labels:
        app: {{ include "dramaflow-backend.fullname" . }}-content
    spec:
      containers:
        - name: content-service
          image: {{ .Values.services.content.image }}
          imagePullPolicy: {{ .Values.global.imagePullPolicy }}
          ports:
            - containerPort: {{ .Values.services.content.port }}
          envFrom:
            - configMapRef:
                name: {{ include "dramaflow-backend.fullname" . }}-config
          env:
            - name: DRAMAFLOW_POSTGRES_DSN
              value: {{ .Values.global.secrets.postgresDsn | quote }}
            - name: DRAMAFLOW_JWT_SECRET
              value: {{ .Values.global.secrets.jwtSecret | quote }}
          readinessProbe:
            httpGet:
              path: /health/ready
              port: {{ .Values.services.content.port }}
          livenessProbe:
            httpGet:
              path: /health/live
              port: {{ .Values.services.content.port }}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "dramaflow-backend.fullname" . }}-content
spec:
  selector:
    app: {{ include "dramaflow-backend.fullname" . }}-content
  ports:
    - port: {{ .Values.services.content.port }}
      targetPort: {{ .Values.services.content.port }}
{{- end }}
