# Auth + Feed + Detail

Preconditions:
- Android source checkout restored
- Auth/feed/content/progress staging URLs available

Expected:
- Guest session works
- Feed loads real data
- Detail and episodes show backend data
- Progress is readable and writable
